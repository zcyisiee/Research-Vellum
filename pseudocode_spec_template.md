按照伪代码 spec，生成主流程伪代码。

【算法名称】：[算法的学术或通用名称]

【0. 目的与边界】
做什么: [一句话：本脚本完成的最小实验单元，重要的地方需要用形式化语言说明]

【1. 输入与超参】
1a. 可搜索超参（数值，允许第二阶段搜索 Agent 调节）

| 变量名 | 描述 | 默认值 |
|---|---|---|
| [变量名] | [描述] | [默认值] |
| ... | ... | ... |

1b. 实验开关（语义固定，非消融时不得改动）

| 变量名 | 描述 | 取值 |
|---|---|---|
| [变量名] | [描述] | [取值] |
| ... | ... | ... |

1c. 运行时输入（外部给定的数据 / 对象）

| 变量名 | 描述 |
|---|---|
| [变量名] | [描述及数据类型 / 维度] |
| ... | ... |

【2. 主控制流】
循环 [条件]:
    1. [用一句话解释这一步的物理意义或目的，只有核心函数需要显式写出并说明，非核心部分只需要说明目的即可]
       [变量] = [函数名]([参数])
       
    2. [逻辑判断或状态更新]
       如果 [条件]:
           [更新状态]

【3. 维护状态】

| 变量名 | 描述 | 初始值 |
|---|---|---|
| [变量名] | [描述] | [初始值] |
| ... | ... | ... |

【4. 核心函数】

每个函数按以下格式呈现（子标题描述功能而非函数名，函数名已在 signature 中体现）：

### 4.x [该函数的功能描述，一句话概括]

```python
def function_name(
    param: Type,        # 行内注释：shape / 语义说明
    ...
) -> ReturnType:        # 行内注释：shape / 语义说明
```
- **输出:** [返回值的一句话描述]
- **逻辑:**
  1. [运算步骤]
  2. ...
- **形式化:** [如果需要，写清晰的数学表达式；否则省略此项]

说明：
- signature 必须包含完整的 Python type hint，包括张量的 shape 注释。
- 子标题写功能描述（如"计算对抗后缀的坐标梯度"），不重复函数名。
- 输入信息全部体现在 signature 中，不再单独写"输入"字段。
- "输出"只写一句话描述，不需要重复 type / shape（已在 signature 的返回值注释中体现）。

【5. Gotcha 账本】（人类可读的制度记忆，每条踩过的坑一项，初始化脚本时无需编写。）
- G1: [G1 的概述]
  - 表现: [可观测到的错误现象]
  - 原因: [为什么会这样]
  - 规则: [正确做法]
  - 对应自检: [链接到【5】中的 I 编号；若无法断言则注"仅经验"]
  - 更新时间: [哪次实验 / 日期 surfaced 出来的]
- G2
  - ...

# 伪代码 Spec 模板

`docs/PSEUDOCODE-{name}.md` 的标准结构。它是实验逻辑的**顶层叙事**——描述实验在做什么、
数据如何流转、关键状态如何变化，而非代码如何组织。研究员读懂并批准这份伪代码后，
AI 据此实现 `core/` 与 `exp/`（函数划分由 AI 决定）；research-audit 以本文件
为 claim 的主要来源。

每个伪代码文件以 YAML frontmatter 开头，然后按 §0–§5 填写。
§0/§1/§2 必填；§3 仅在有跨步骤的核心公式时写；§4/§5 按需。

---

```yaml
---
title: [实验脚本的一句话描述]
entry_scripts:
  - [对应的主入口脚本路径]
created: YYYY-MM-DD
modified: YYYY-MM-DD
status: draft | reviewed | verified
---
```

【算法名称】：[算法的学术或通用名称]

## 【0. 目的与边界】

**做什么:** [一句话：本脚本完成的最小实验单元，关键处用形式化语言写清]

**边界:** [本脚本负责什么、不负责什么；与其它脚本的分工]

## 【1. 输入与超参】

### 1a. 可搜索超参（数值，允许第二阶段搜索 Agent 调节）

| 变量名 | 描述 | 默认值 |
|---|---|---|
| [变量名] | [描述] | [默认值]

### 1b. 实验开关（语义固定，非消融时不得改动）

| 变量名 | 描述 | 取值 |
|---|---|---|
| [变量名] | [描述] | [取值]

### 1c. 运行时输入（外部给定的数据 / 对象）

| 变量名 | 描述 |
|---|---|
| [变量名] | [描述及数据类型 / 维度] |

> 1a 与 1b 的区分是给第二阶段超参搜索用的硬边界：搜索 Agent 只能动 1a，碰 1b 即违规。

## 【2. 主控制流】

用自然语言描述每一步的**物理意义**。可附带关键变量名，但不写完整函数签名。
核心公式可内联一行；复杂公式放 §3。

```
1. [这一步做什么、为什么做]
2. [下一步]
   如果 [条件]:
       [分支行为]
3. 循环 [条件]:
    a. [循环体内每一步的物理意义]
    b. [状态更新或条件判断]
```

## 【3. 关键算法细节】（可选）

放跨越多个步骤的核心数学公式或算法 trick。不写函数签名，不写逐步代码。
大多数实验不需要此节——只有当 §2 中反复出现同一个公式的不同侧面时才提取到这里。

### 3.x [算法/公式的名称或一句话概括]

[一段话说清这个 trick 的本质和直觉]

$$[核心公式]$$

[公式中关键符号的含义，以及它在 §2 哪些步骤中起作用]

## 【4. 维护状态】

| 变量名 | 描述 | 初始值 |
|---|---|---|
| [变量名] | [描述] | [初始值]

## 【5. Gotcha 账本】

人类可读的制度记忆，每条踩过的坑一项。初始化时无需编写，随实验迭代追加；与
`docs/MISTAKES.md` 保持同源（这里写"规则"，MISTAKES.md 写完整复盘）。

- G1: [概述]
  - 表现: [可观测到的错误现象]
  - 原因: [为什么会这样]
  - 规则: [正确做法]
  - 对应自检: [链接到 research-develop 写的 assert；若无法断言则注"仅经验"]
  - 更新时间: [哪次实验 / 日期暴露出来]
